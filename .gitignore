# OS / editors
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp

# Node (the hooks have no dependencies, but just in case)
node_modules/
*.log

# Internal handoff document: do not publish (private config and paths)
HANDOFF.md
